import React, { useState } from 'react';
import { MultipleFilters } from  '../../components/FilterBar'

const dashboards = () => {
    return( 
        <MultipleFilters/>
    )
};


export default dashboards