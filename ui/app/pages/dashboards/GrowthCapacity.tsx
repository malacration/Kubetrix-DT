import React from 'react';
import { GrowthCapacityPanel } from 'app/components/widget/cluster/GrowthCapacityPanel';

// The annual analysis deliberately ignores global timeframe, resolution and auto-refresh.
export default function GrowthCapacity() {
  return <GrowthCapacityPanel />;
}
